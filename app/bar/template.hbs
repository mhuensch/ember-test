<form {{action 'save' model on="submit"}}>
	{{input value=model.name}}

	<div>
	<button type="submit" class="{{unless isDirty "invisible"}}">Save</button>
	<button {{action 'delete' model}} class="{{unless model.id "invisible"}}">Delete</button>
	</div>
</form>
